<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://code.jquery.com/jquery-3.6.1.min.js" integrity="sha256-o88AwQnZB+VDvE9tvIXrMQaPlFFSUTR+nldQm1LuPXQ=" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/0.27.2/axios.min.js" integrity="sha512-odNmoc1XJy5x1TMVMdC7EMs3IVdItLPlCeL5vSUPN2llYKMJ2eByTTAIiiuqLg+GdNr9hF6z81p27DArRFKT7A==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <title>Document</title>
    <style>
        #info_birth {
            display: flex;
        }
        #info_birth select {
            margin-left: 7px;
        }
        #info_birth select:first-child {
            margin-left: 0px;
        }
    </style>
</head>
<body>
    <form id="form_register">
        이름 <input type="text" name="name" required> <br>
        성별 <input type="radio" name="gender" value="남" required> 남자
        <input type="radio" name="gender" value="여" required> 여자 <br>
        생년원일 <select>
            <%for (let i = 1940; i <= 2022; i++) {%>
                <option name="birthYear" value="<%=i%>"><%=i%>년</option>
                <%}%>
        </select>
        <select>
            <%for (let j = 1; j <= 12; j++) {%>
                <option name="birthMonth" value="<%=j%>"><%=j%>월</option>
                <%}%>
        </select>
        <select>
            <%for (let k = 1; k <= 31; k ++) {%>
                <option name="birthDay" value="<%=k%>"><%=k%>일</option>
                <%}%>
        </select><br>
        관심사 <input type="checkbox" name="hobby" value="trip">여행
        <input type="checkbox" name="hobby" value="fashion">패션
        <input type="checkbox" name="hobby" value="food">음식<br>
        <button type="button" onclick="dynamicForm();">회원가입</button>
    </form>
    <div></div>
    <script>
        function dynamicForm() {
            var form = document.querySelector("form");
            var data1 = {
                name: form.name.value,
                gender: form.gender.value,   
            }
            axios({
                url: "get/axios",
                method: "get", // post
                params: data1 // data
            }).then((response) => {
                console.log(response)
            })
        }
    </script>
</body>
</html>